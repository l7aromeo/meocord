import { UserContextMenuCommandInteraction, MessageContextMenuCommandInteraction } from 'discord.js'
import { respond } from 'meocord/common'
import { Command, Controller } from 'meocord/decorator'
import { {{builderClassName}} } from '{{builderImportPath}}'

@Controller()
export class {{className}}ContextMenuController {
  @Command('{{commandName}}', {{builderClassName}})
  async handle{{className}}(interaction: UserContextMenuCommandInteraction | MessageContextMenuCommandInteraction) {
    await respond(interaction).send('Hello from {{commandName}}!')
  }
}
