import { ModalSubmitInteraction } from 'discord.js'
import { respond } from 'meocord/common'
import { Command, Controller } from 'meocord/decorator'
import { CommandType } from 'meocord/enum'

@Controller()
export class {{className}}ModalSubmitController {
  // A modal whose customId is {{commandName}}
  @Command('{{commandName}}', CommandType.MODAL_SUBMIT)
  async handle{{className}}(interaction: ModalSubmitInteraction) {
    await respond(interaction).send('Modal submitted!')
  }
}
