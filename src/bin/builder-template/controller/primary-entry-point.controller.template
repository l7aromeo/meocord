import { PrimaryEntryPointCommandInteraction } from 'discord.js'
import { respond } from 'meocord/common'
import { Command, Controller } from 'meocord/decorator'
import { {{builderClassName}} } from '{{builderImportPath}}'

@Controller()
export class {{className}}PrimaryEntryPointController {
  @Command('{{commandName}}', {{builderClassName}})
  async handle{{className}}(interaction: PrimaryEntryPointCommandInteraction) {
    await respond(interaction).send('Launching the activity.')
  }
}
