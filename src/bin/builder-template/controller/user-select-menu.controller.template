import { UserSelectMenuInteraction } from 'discord.js'
import { respond } from 'meocord/common'
import { Command, Controller } from 'meocord/decorator'
import { CommandType } from 'meocord/enum'

@Controller()
export class {{className}}UserSelectMenuController {
  // A menu whose customId is {{commandName}}
  @Command('{{commandName}}', CommandType.USER_SELECT_MENU)
  async handle{{className}}(interaction: UserSelectMenuInteraction) {
    await respond(interaction).send({ content: `Selected: ${interaction.users.map(user => user.username).join(', ')}` })
  }
}
