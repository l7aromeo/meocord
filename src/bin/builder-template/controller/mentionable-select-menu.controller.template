import { MentionableSelectMenuInteraction } from 'discord.js'
import { respond } from 'meocord/common'
import { Command, Controller } from 'meocord/decorator'
import { CommandType } from 'meocord/enum'

@Controller()
export class {{className}}MentionableSelectMenuController {
  // A menu whose customId is {{commandName}}
  @Command('{{commandName}}', CommandType.MENTIONABLE_SELECT_MENU)
  async handle{{className}}(interaction: MentionableSelectMenuInteraction) {
    await respond(interaction).send({
      content: `Selected ${interaction.users.size} user(s) and ${interaction.roles.size} role(s).`,
    })
  }
}
